TODO LIST:


interrupt.h:
	zB MIR0 / ITR0 umbauen zu MIR(n) / ITR(n) 
	
interrupt_controller.h:
	funktions deklarationen einfügen
	
interrupt_controller.c:
	wrapper funktionen für zB enable_mir_(mir nummer) -> enable__mir_(1);
	wrapper für andere asdf(0)
	interrupt_mode -> für irq oder fiq
	
	
timer.h:
	funk. dekl. einfügen
	enum für trigger modes ausbessern
	sinnvolle enums einaben wo möglich (zB rising/falling edge?)

timer.c:
	funktion "fast_init" wo _alles_ konfiguriert sodass nurnoch start_timer aufgerufen werden muss
	enums für sachen wie compare_mode
	
	
	
MMU
regions-pagesize in kb
aktiviere pagetables --> master-page-table-pointer in TTB eitnragen
alles mit fault entries füllen
	init (slide 28)